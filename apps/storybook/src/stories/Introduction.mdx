import { Meta } from '@storybook/addon-docs/blocks';

<Meta title='Getting Started' />

# Frey UI

A strict, accessible design system library for internal applications.

## What this Storybook is for

- Browse live component examples and states
- Verify behavior across light and dark themes
- Use Controls and a11y checks while developing

## Quick start

```bash
pnpm add frey-ui
```

```tsx
import 'frey-ui/style.css';
import { ThemeProvider, Button } from 'frey-ui';

export function Example() {
  return (
    <ThemeProvider theme='system' highContrast={true}>
      <Button>Save changes</Button>
    </ThemeProvider>
  );
}
```

## Components in this library

- Alert
- Button
- Checkbox
- Chip
- Dialog
- DropdownMenu
- Field
- Icons
- Popover
- RadioGroup
- Progress
- Select
- Skeleton
- Spinner
- Switch
- Textarea
- TextInput
- Toast (`ToastProvider` + `useToast`)
- Tooltip
- ThemeProvider

## Usage patterns

Explore practical composition examples in `Patterns/Usage Patterns`:

- Forms
- Settings page
- Auth form
- Table filters

## Dialog pattern

Dialog examples use an explicit trigger button and controlled `open` state. This keeps the dialog closed by default and matches native `<dialog>` usage (`showModal()` + close actions).

## Overlay and feedback primitives

- `Popover`, `Tooltip`, and `DropdownMenu` use trigger-first patterns with keyboard support and outside-dismiss defaults.
- `ToastProvider` + `useToast` expose an imperative API for notifications.
- `Progress` and `Spinner` provide accessible loading and completion feedback states.

## Icon tokens

`Icons` support named size/stroke tokens and numeric overrides:

- `size`: `xs` | `sm` | `md` | `lg` | `xl` or a number
- `strokeWidth`: `thin` | `regular` | `bold` or a number

## Full package docs

For full API and usage documentation, see `packages/frey-ui/README.md`.
