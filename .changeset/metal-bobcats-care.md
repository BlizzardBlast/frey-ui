---
'frey-ui': patch
---

Normalize package module outputs to explicit `.mjs`/`.cjs`, replace wildcard subpath exports with explicit component-only exports, and remove the unused root Turborepo `tsconfig.json` global dependency.
